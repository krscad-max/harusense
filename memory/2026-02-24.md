2026-02-24 — Durable session notes
Time: 2026-02-24 05:28 America/Edmonton

- Merged and deployed latest nosaic-note/lotto.html updates (multiple iterative patches) to gh-pages. Changes include:
  - Full UI for Basic and Custom modes (tabs, forms, buttons).
  - Extended "correlationRules" for Custom mode: many animal/object rules, country and city mappings, name/city/extra-derived numeric rules, birth-derived month/day/sum rules, de-duplication and limit to top rules.
  - Deterministic seed-based RNG (hash32 + mulberry32) so identical inputs yield identical sets.
  - Custom generation logic: must/avoid handling, weighted correlation pool, style pools, per-number reasons, mystic one-line messages, shuffled selection with seeded randomness.
  - Basic generation logic: sample draws fallback, frequency-based pools (>=10, >=5 & recent3), and per-number reasons for basic sets.
  - UI styling updates (dark/fancy -> lighter gradient theme present in latest file), responsive form grid, accessibility attributes on tabs.
  - File updated: /Users/sj/.openclaw/workspace/web-lotto/nosaic-note/lotto.html (committed and pushed to main). Published to gh-pages (forced push).

- Deployment notes:
  - Live URL: https://krscad-max.github.io/sj-web/nosaic-note/lotto.html (HTTP 200 verified after each publish).
  - Publish workflow uses staging dir: /Users/sj/.openclaw/workspace/tmp_publish_final and force-push to gh-pages.

- Local repository state/items to consider:
  - Backup files left in workspace: web-lotto/nosaic-note/index.html.bak and web-lotto/nosaic-note/lotto.html.bak (assistant offered to delete if requested).
  - draws.json not yet added; assistant suggested adding real draw history to improve Basic recommendations.
  - A workspace backup ZIP exists from earlier session: /Users/sj/.openclaw/workspace/workspace-backup-20260223-223217.zip (keep until user confirms deletion).

- Automation/fetcher context reminder:
  - fetch_magayo.py exists and runs via run_fetch.sh cron plan; parser saves debug snapshots on failures; Telegram alerting configured in script (tokens stored in .env locally).

- Next recommended actions (user decision required):
  - Remove .bak files from repo/workspace.
  - Commit and upload draws.json (format: array of [n1..n6] or objects with round/nums) so client uses real historical draws.
  - Clean repository history if any secrets leaked (requires confirmation to run git-filter-repo/BFG and collaborators to re-clone).
  - Cloudflare Pages migration or Netlify setup (requires short-lived API token or manual UI steps).

Recorded by: OpenClaw assistant

